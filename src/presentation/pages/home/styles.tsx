import styled from 'styled-components';
import { Logo as BaseLogo } from '@/presentation/components';

export const Container = styled.main`
  width: 100%;
  padding: 20px;
`;

export const Logo = styled(BaseLogo)`
  margin-bottom: 20px;
`;
